# Browser-visible API configuration. Do not put secrets in VITE_* variables.
VITE_API_BASE_URL=auto

# Production builds use the real backend. Set to true for frontend-only mock demos.
VITE_USE_MOCK_API=false
